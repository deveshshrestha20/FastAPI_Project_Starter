"""
Database module switcher
Automatically selects async or sync DB based on project configuration
Generated by FastGen
"""

from app.core.config import settings

{% if is_async %}
# Async DB selected
from app.db.async_db import get_db, init_db, close_db, get_session, load_models
{% else %}
# Sync DB selected
from app.db.sync_db import get_db, init_db, close_db, get_session, load_models
{% endif %}